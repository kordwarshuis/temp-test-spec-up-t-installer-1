name: Temp test-spec-up-t-installer-1
on:
  workflow_dispatch:  # Allows manual triggering
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: trustoverip/spec-up-t-starter-pack@main


